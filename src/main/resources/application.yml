spring:
  application:
    name: oanse
  datasource:
    url: jdbc:postgresql://localhost:5432/oanse
    username: postgres
    password: 123
    driver-class-name: org.postgresql.Driver
    jpa:
      show-sql: true
    flyway:
      baseline-on-migrate: true
    security:
      user: #credenciais para request base64 encoded
        name: oanse 
        password: 123456
  #profiles:
    #active: test
server:
  port: 8081