spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/oanse_test
    username: postgres
    password: senha123
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true